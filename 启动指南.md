# PR Review 应用启动指南

## 1. 环境准备

### 必需的服务
- **PostgreSQL 数据库**（默认端口 5432）
- **Java 21**
- **Maven**

### 必需的 API 密钥
- **DashScope API Key**（阿里云通义千问）或 OpenAI API Key
- **GitHub Token**（用于访问 GitHub API）

## 2. 配置 API 密钥

### 方式一：使用环境变量（推荐）

在启动应用前设置以下环境变量：

```bash
# Windows (PowerShell)
$env:PRREVIEW_API_KEY="your-custom-api-key"
$env:DASHSCOPE_API_KEY="your-dashscope-api-key"
$env:GITHUB_TOKEN="your-github-token"

# Windows (CMD)
set PRREVIEW_API_KEY=your-custom-api-key
set DASHSCOPE_API_KEY=your-dashscope-api-key
set GITHUB_TOKEN=your-github-token

# Linux/Mac
export PRREVIEW_API_KEY="your-custom-api-key"
export DASHSCOPE_API_KEY="your-dashscope-api-key"
export GITHUB_TOKEN="your-github-token"
```

### 方式二：修改配置文件

编辑 `prreview-web/src/main/resources/application.yml`：

```yaml
prreview:
  api-key: your-custom-api-key  # 修改这里

spring:
  ai:
    openai:
      api-key: your-dashscope-api-key  # 修改这里

prreview:
  github:
    token: your-github-token  # 修改这里
```

### 默认 API 密钥
如果不设置，应用会使用默认值：
- **PRREVIEW_API_KEY**: `dev-api-key-change-in-production`

## 3. 启动数据库

确保 PostgreSQL 正在运行，并创建数据库：

```sql
CREATE DATABASE prreview;
CREATE USER prreview WITH PASSWORD 'prreview';
GRANT ALL PRIVILEGES ON DATABASE prreview TO prreview;
```

或者使用环境变量自定义数据库连接：

```bash
$env:DB_URL="jdbc:postgresql://localhost:5432/prreview"
$env:DB_USERNAME="prreview"
$env:DB_PASSWORD="prreview"
```

## 4. 启动应用

### 方式一：使用 Maven 启动

```bash
cd prreview
mvn spring-boot:run -pl prreview-web
```

### 方式二：打包后启动

```bash
# 打包
mvn clean package -DskipTests

# 运行
java -jar prreview-web/target/prreview-web-0.1.0-SNAPSHOT.jar
```

## 5. 测试 API

应用启动后，默认运行在 `http://localhost:8080`

### 查看 API 文档
访问 Swagger UI：
```
http://localhost:8080/swagger-ui.html
```

### 测试提交审查请求

使用 curl 或 Postman：

```bash
curl -X POST http://localhost:8080/api/reviews/v1 \
  -H "Content-Type: application/json" \
  -H "X-API-Key: dev-api-key-change-in-production" \
  -H "Idempotency-Key: $(uuidgen)" \
  -d '{
    "repository": "octocat/hello-world",
    "pullRequestNumber": 42,
    "analysisProfile": "STANDARD"
  }'
```

**Windows PowerShell 版本：**

```powershell
$headers = @{
    "Content-Type" = "application/json"
    "X-API-Key" = "dev-api-key-change-in-production"
    "Idempotency-Key" = [guid]::NewGuid().ToString()
}

$body = @{
    repository = "octocat/hello-world"
    pullRequestNumber = 42
    analysisProfile = "STANDARD"
} | ConvertTo-Json

Invoke-RestMethod -Uri "http://localhost:8080/api/reviews/v1" `
    -Method POST `
    -Headers $headers `
    -Body $body
```

### 查看审查状态

```bash
curl -X GET http://localhost:8080/api/reviews/v1/{reviewId}/status \
  -H "X-API-Key: dev-api-key-change-in-production"
```

## 6. 重要配置说明

### API 密钥位置
- **配置文件**: `prreview-web/src/main/resources/application.yml` 第 54 行
- **环境变量**: `PRREVIEW_API_KEY`

### 请求头要求
所有 API 请求必须包含：
- `X-API-Key`: 你配置的 API 密钥
- `Content-Type: application/json`（POST 请求）
- `Idempotency-Key`: UUID（POST 请求，可选但推荐）

### 端口配置
默认端口是 8080，可以通过环境变量修改：
```bash
$env:SERVER_PORT=9090
```

## 7. 常见问题

### 数据库连接失败
- 确保 PostgreSQL 正在运行
- 检查数据库名称、用户名、密码是否正确
- 确认防火墙允许 5432 端口

### API 密钥认证失败（401）
- 检查请求头中的 `X-API-Key` 是否正确
- 确认配置文件或环境变量中的 API 密钥设置

### DashScope API 调用失败
- 确认 `DASHSCOPE_API_KEY` 已正确设置
- 检查网络连接到阿里云服务

### GitHub API 调用失败
- 确认 `GITHUB_TOKEN` 已正确设置
- 检查 token 权限是否包含读取仓库和 PR 的权限
